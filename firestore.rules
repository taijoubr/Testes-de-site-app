rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global default deny
    match /{document=**} {
      allow read, write: if false;
    }

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isValidId(id) {
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    function incoming() {
      return request.resource.data;
    }

    function existing() {
      return resource.data;
    }

    // Connection check document
    match /_connection_check/{docId} {
      allow read, write: if true;
    }

    // Quotes collection
    match /quotes/{quoteId} {
      allow read, write: if true;
    }

    // Proposals collection
    match /proposals/{proposalId} {
      allow read, write: if true;
    }

    // Projects collection
    match /projects/{projectId} {
      allow read, write: if true;
    }

    // Financials collection
    match /financials/{financialId} {
      allow read, write: if true;
    }

    // Chat messages collection
    match /chatMessages/{messageId} {
      allow read, write: if true;
    }

    // Tickets collection
    match /tickets/{ticketId} {
      allow read, write: if true;
    }

    // Leads collection
    match /leads/{leadId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    // Notifications collection
    match /notifications/{notificationId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    // Admin Users collection
    match /adminUsers/{adminUserId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    // Client Users collection
    match /clientUsers/{clientUserId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    // Site Settings collection (public configuration for site header, hero, contact)
    match /siteSettings/{settingsId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    // Client Subscriptions collection (mensalidades e serviços recorrentes)
    match /clientSubscriptions/{subscriptionId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    // Services collection (catálogo de serviços públicos)
    match /services/{serviceId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    // Contracts collection (Contratos de prestação de serviços gerados)
    match /contracts/{contractId} {
      allow read: if true;
      allow create, update, delete: if true;
    }
  }
}
